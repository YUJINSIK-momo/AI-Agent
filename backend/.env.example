PORT=4000
GITHUB_TOKEN=your_github_personal_access_token_here
# Postgres 연결 문자열 (Supabase Dashboard → Database → URI). 미설정 시 health의 database.connected는 false.
SUPABASE_URL=postgresql://user:password@host:6543/postgres
